{
    "name": "@cowchain/redis",
    "version": "0.0.0",
    "main": "dist/index.js",
    "private": true,
    "scripts": {
        "build": "rimraf dist && rimraf *.tsbuildinfo && tsc"
    },
    "dependencies": {
        "@cowchain/exceptions": "*",
        "@cowchain/tsconfig": "*",
        "@nestjs/common": "^10.0.0",
        "ioredis": "^5.3.2",
        "typescript": "5.1.3"
    },
    "devDependencies": {
        "rimraf": "^5.0.1"
    }
}