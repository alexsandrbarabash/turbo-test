{
    "name": "@cowchain/database",
    "version": "0.0.0",
    "private": true,
    "main": "dist/index.js",
    "scripts": {
        "build": "rimraf dist && rimraf *.tsbuildinfo && tsc"
    },
    "dependencies": {
        "@cowchain/exceptions": "*",
        "@cowchain/tsconfig": "*",
        "@nestjs/common": "^10.0.0",
        "@nestjs/typeorm": "^10.0.0",
        "typeorm": "0.3.16",
        "typescript": "5.1.3"
    },
    "devDependencies": {
        "rimraf": "^5.0.1"
    }
}